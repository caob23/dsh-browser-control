# Security Policy

## 权限说明

本扩展请求以下 Chrome 权限：

| 权限 | 用途 |
|---|---|
| `debugger` | 通过 CDP 驱动页面（Runtime.evaluate、Input 事件、截图） |
| `tabs` | 读取标签页信息、管理标签页导航 |
| `storage` | 保存用户配置（端口、Token、自动连接） |
| `alarms` | 保活心跳，维持 WebSocket 连接 |

## 安全边界

- **仅监听 127.0.0.1**：桥接服务不暴露到网络
- **Token 认证**：扩展连接时需携带正确 Token
- **本地通信**：扩展 ↔ 桥仅通过 WebSocket 在本机通信
- **无数据上传**：不向任何外部服务器发送数据
- **用户控制**：插件默认关闭，需手动在 dsh 设置中开启

## 已知限制

- 扩展可通过 CDP 执行页面上的任意 JavaScript
- 桥接的 `/api/command` 端点接受 JSON 命令，无额外鉴权（依赖 127.0.0.1 绑定）
- 同一网络的其他进程无法连接（127.0.0.1 限制），但同机的其他程序理论上可访问

## 报告安全问题

如发现安全漏洞，请通过 GitHub Issues 报告或联系作者。

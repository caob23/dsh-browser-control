# dsh-browser-bridge: mounts disabled by default.
# Enable via Settings → Plugins → DSH Browser Control.
- insert:
    - id: browser-bridge
      name: '@deepseek-ai/dsh-browser-bridge'
      config:
        enabled: false
        port: 9777
        token: dsh-local
